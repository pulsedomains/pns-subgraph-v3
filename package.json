{
  "name": "pns-subgraph",
  "version": "2.0.0",
  "repository": "https://github.com/pnsdomains/pns-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "setup": "yarn codegen && yarn create-local && yarn deploy-local",
    "local:create": "graph create graphprotocol/pns --node http://127.0.0.1:8020",
    "local:deploy": "graph deploy graphprotocol/pns --debug --ipfs http://127.0.0.1:5001 --node http://127.0.0.1:8020/ --version-label 0.0.1",
    "local:remove": "graph remove graphprotocol/pns --node http://127.0.0.1:8020",
    "server:setup": "yarn codegen && yarn server:create-local && yarn server:deploy-local",
    "server:create": "graph create graphprotocol/pns-v3 --node https://graph.pulse.domains",
    "server:deploy": "graph deploy graphprotocol/pns-v3 --debug --ipfs https://ipfs.pulse.domains --node http://graph.pulse.domains --version-label 1.0.0",
    "server:remove": "graph remove graphprotocol/pns-v3 --node https://graph.pulse.domains",
    "build": "graph build",
    "deploy": "graph deploy graphprotocol/pns-v3 --ipfs https://ipfs.pulse.domains --node https://graph.pulse.domains"
  },
  "devDependencies": {
    "@ensdomains/content-hash": "^2.5.3",
    "@graphprotocol/graph-cli": "^0.23.2",
    "@graphprotocol/graph-ts": "^0.23.1"
  }
}
